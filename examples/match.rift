let
  fn f(n: i64): string =
    match n with
      | 1 => "one"
      | 2 => "two"
      | 3 => "three"
      | 4 => "four"
      | 5 => "five"
      | else => "something else"
in
  print(f(42))
end
